export const dynamic = "force-dynamic";

export default function DevLoginLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <>
            {children}
        </>
    );
}
