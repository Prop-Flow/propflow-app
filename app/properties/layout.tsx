export const dynamic = "force-dynamic";

export default function PropertiesLayout({
    children,
}: {
    children: React.ReactNode;
}) {
    return (
        <>
            {children}
        </>
    );
}
