@@ -133,18 +133,28 @@
                     await new Promise(r => setTimeout(r, 800)); // Simulate processing time
                 }
 
-                setAnalysisStep('Seeding demo data...');
+                setAnalysisStep('Activating demo account...');
                 
-                // Call demo seed API
-                const seedResponse = await fetch('/api/demo/seed', {
+                // Get user token for activation
+                const { getAuth } = await import('firebase/auth');
+                const auth = getAuth();
+                const currentUser = auth.currentUser;
+                if (!currentUser) {
+                    throw new Error('User not authenticated');
+                }
+                const token = await currentUser.getIdToken();
+
+                // Call demo activation API
+                const activateResponse = await fetch('/api/demo/activate', {
                     method: 'POST',
+                    headers: {
+                        'Authorization': `Bearer ${token}`,
+                    },
                 });
 
-                if (!seedResponse.ok) {
-                    throw new Error('Failed to seed demo data');
+                if (!activateResponse.ok) {
+                    throw new Error('Failed to activate demo');
                 }
-
-                const seedResult = await seedResponse.json();
                 
                 setAnalysisStep('Success! Loading your dashboard...');
 
